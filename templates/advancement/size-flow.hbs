<div>
    {{~#if size}}
    <fieldset>
        <legend>{{ localize "VARLYN5E.ADVANCEMENT.Size.Action.SelectSize" }}</legend>
        {{ formField size.field name="size" value=size.value options=size.options }}
    </fieldset>
    {{/if~}}
</div>
