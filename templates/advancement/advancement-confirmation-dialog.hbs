<section class="flexcol">
    <p class="note warn">{{ content }}</p>
    <fieldset>
        <legend>{{ localize "VARLYN5E.ADVANCEMENT.Deletion.Options" }}</legend>
        <div class="form-group">
            <label for="{{ partId }}-apply">{{ localize "VARLYN5E.ADVANCEMENT.Deletion.Label" }}</label>
            <div class="form-fields">
                <dnd5e-checkbox id="{{ partId }}-apply" name="apply-advancement" checked></dnd5e-checkbox>
            </div>
        </div>
    </fieldset>
</section>
