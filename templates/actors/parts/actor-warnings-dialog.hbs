<dialog class="warnings locked-tooltip varlyn5e-tooltip varlyn5e active themed theme-light">
    <ul class="unlist">
        {{#each warnings}}
        <li class="{{ type }}">
            {{#if link}}<a data-action="inspectWarning" data-target="{{ link }}">{{/if}}
            {{ message }}
            {{#if link}}</a>{{/if}}
        </li>
        {{/each}}
    </ul>
</dialog>
