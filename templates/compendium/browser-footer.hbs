<div class="flexrow">
    {{~#if displaySelection}}
    <div class="count {{#if invalid}}invalid {{/if}}">
        {{{ localize "VARLYN5E.CompendiumBrowser.Selection.Label" summary=summary }}}
    </div>
    <button type="submit"{{#if invalid}} disabled data-tooltip aria-label="{{invalidTooltip}}" {{/if}}>
        {{ localize "VARLYN5E.CompendiumBrowser.Selection.Select" }}
    </button>
    {{/if~}}
</div>
